import {createContext} from 'react';

export const SearchDataContext = createContext({});

export const FourPlayerURLsContext = createContext({});

export const WhichPlayerToReplace = createContext({});